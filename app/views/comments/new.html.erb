<h1>New comment</h1>
<%= form_for [@society, @event, @comment], url: {action: "create"} do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, class:"description_field"%>
  </div><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
